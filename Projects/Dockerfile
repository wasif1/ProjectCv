# Step 1: Choose a base image (for Java apps)
FROM openjdk:17-jdk-slim

# Step 2: Set the working directory in the container
WORKDIR /Projects

# Step 3: Copy the JAR file generated by Spring Boot into the container
COPY build/libs/Projects-1.0.jar app.jar

# Step 4: Expose the port used by the microservice
EXPOSE 8082

# Step 5: Command to run the JAR file
ENTRYPOINT ["java", "-jar", "app.jar"]